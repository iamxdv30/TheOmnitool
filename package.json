{
  "name": "theomnitool",
  "version": "1.0.0",
  "private": true,
  "description": "Root build shim for dual-stack Heroku deployment (Flask + Next.js)",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm ci --include=dev && npm run build"
  }
}
